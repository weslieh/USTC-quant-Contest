# Runtime dependencies for the submitted strategy (inference only).
# The model is LightGBM (backend=lgb); xgboost/catboost/torch are imported only
# inside backend-specific branches that this model never hits, so they are not
# required at inference time. Install in the JupyterHub base env before submit:
#   python -m pip install -r requirements.txt
lightgbm==4.6.0
numpy==2.1.3
pandas==3.0.3
